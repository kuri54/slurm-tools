[project]
name = "sltop"
version = "0.2.0"
description = "Simple Slurm resource viewer"
requires-python = ">=3.10"

[project.scripts]
sltop = "sltop.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
